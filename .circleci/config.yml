version: 2.1

executors:
  job-runner:
    docker:
      - image: circleci/golang:1.12
    working_directory: /go/src/github.com/{{ORG_NAME}}/{{REPO_NAME}}
    environment:
      GO111MODULE: "on"
jobs:
  tests:
    executor: job-runner
    steps:
      - checkout
      # specify any bash command here prefixed with `run: `
      - run: go test -v ./...
  scraper-image:
    # specify the version
    executor: job-runner
    environment:
      SCRAPER_IMAGE_NAME: codesmag/scraper
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker Image
          command: docker build -f scraper/Dockerfile -t $SCRAPER_IMAGE_NAME .
      - run:
          name: Publish Docker Image to Docker Hub
          command: |
            echo "$DOCKERHUB_PASS" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            IMAGE_TAG=$(date "+%Y-%m-%dT%H-%M-%S")
            docker tag $SCRAPER_IMAGE_NAME:latest $SCRAPER_IMAGE_NAME:$IMAGE_TAG
            docker push $SCRAPER_IMAGE_NAME:latest
            docker push $SCRAPER_IMAGE_NAME:$IMAGE_TAG
  postgres-inserter-image:
    # specify the version
    executor: job-runner
    environment:
      SCRAPER_IMAGE_NAME: codesmag/postgres-inserter
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker Image
          command: docker build -f postgres-inserter/Dockerfile -t $SCRAPER_IMAGE_NAME .
      - run:
          name: Publish Docker Image to Docker Hub
          command: |
            echo "$DOCKERHUB_PASS" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            IMAGE_TAG=$(date "+%Y-%m-%dT%H-%M-%S")
            docker tag $SCRAPER_IMAGE_NAME:latest $SCRAPER_IMAGE_NAME:$IMAGE_TAG
            docker push $SCRAPER_IMAGE_NAME:latest
            docker push $SCRAPER_IMAGE_NAME:$IMAGE_TAG
  insta-comments-inserter:
    # specify the version
    executor: job-runner
    environment:
      SCRAPER_IMAGE_NAME: codesmag/insta-comments-inserter
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker Image
          command: docker build -f insta-comments-inserter/Dockerfile -t $SCRAPER_IMAGE_NAME .
      - run:
          name: Publish Docker Image to Docker Hub
          command: |
            echo "$DOCKERHUB_PASS" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            IMAGE_TAG=$(date "+%Y-%m-%dT%H-%M-%S")
            docker tag $SCRAPER_IMAGE_NAME:latest $SCRAPER_IMAGE_NAME:$IMAGE_TAG
            docker push $SCRAPER_IMAGE_NAME:latest
            docker push $SCRAPER_IMAGE_NAME:$IMAGE_TAG
  insta-comments-scraper:
    # specify the version
    executor: job-runner
    environment:
      SCRAPER_IMAGE_NAME: codesmag/insta-comments-scraper
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker Image
          command: docker build -f insta-comments-scraper/Dockerfile -t $SCRAPER_IMAGE_NAME .
      - run:
          name: Publish Docker Image to Docker Hub
          command: |
            echo "$DOCKERHUB_PASS" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            IMAGE_TAG=$(date "+%Y-%m-%dT%H-%M-%S")
            docker tag $SCRAPER_IMAGE_NAME:latest $SCRAPER_IMAGE_NAME:$IMAGE_TAG
            docker push $SCRAPER_IMAGE_NAME:latest
            docker push $SCRAPER_IMAGE_NAME:$IMAGE_TAG
  insta-posts-inserter:
    # specify the version
    executor: job-runner
    environment:
      SCRAPER_IMAGE_NAME: codesmag/insta-posts-inserter
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker Image
          command: docker build -f insta-posts-inserter/Dockerfile -t $SCRAPER_IMAGE_NAME .
      - run:
          name: Publish Docker Image to Docker Hub
          command: |
            echo "$DOCKERHUB_PASS" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            IMAGE_TAG=$(date "+%Y-%m-%dT%H-%M-%S")
            docker tag $SCRAPER_IMAGE_NAME:latest $SCRAPER_IMAGE_NAME:$IMAGE_TAG
            docker push $SCRAPER_IMAGE_NAME:latest
            docker push $SCRAPER_IMAGE_NAME:$IMAGE_TAG
  insta-posts-scraper:
    # specify the version
    executor: job-runner
    environment:
      SCRAPER_IMAGE_NAME: codesmag/insta-posts-scraper
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker Image
          command: docker build -f insta-posts-scraper/Dockerfile -t $SCRAPER_IMAGE_NAME .
      - run:
          name: Publish Docker Image to Docker Hub
          command: |
            echo "$DOCKERHUB_PASS" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            IMAGE_TAG=$(date "+%Y-%m-%dT%H-%M-%S")
            docker tag $SCRAPER_IMAGE_NAME:latest $SCRAPER_IMAGE_NAME:$IMAGE_TAG
            docker push $SCRAPER_IMAGE_NAME:latest
            docker push $SCRAPER_IMAGE_NAME:$IMAGE_TAG
  postgres-setup:
    # specify the version
    executor: job-runner
    environment:
      SETUP_IMAGE_NAME: codesmag/postgres-setup
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build Docker Image
          command: docker build -f debezium/Dockerfile -t $SETUP_IMAGE_NAME .
      - run:
          name: Publish Docker Image to Docker Hub
          command: |
            echo "$DOCKERHUB_PASS" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            IMAGE_TAG=$(date "+%Y-%m-%dT%H-%M-%S")
            docker tag $SETUP_IMAGE_NAME:latest $SETUP_IMAGE_NAME:$IMAGE_TAG
            docker push $SETUP_IMAGE_NAME:latest
            docker push $SETUP_IMAGE_NAME:$IMAGE_TAG
workflows:
  version: 2
  workflow:
    jobs:
      - tests
      - scraper-image:
          requires:
            - tests
          filters:
            branches:
              only: master
      - postgres-inserter-image:
          requires:
            - tests
          filters:
            branches:
              only: master
      - insta-comments-inserter:
          requires:
            - tests
          filters:
            branches:
              only: master
      - insta-comments-scraper:
          requires:
            - tests
          filters:
            branches:
              only: master
      - insta-posts-scraper:
          requires:
            - tests
          filters:
            branches:
              only: master
      - insta-posts-inserter:
          requires:
            - tests
          filters:
            branches:
              only: master
      - postgres-setup:
          filters:
            branches:
              only: master
